version: '3.8'

services:
  bot:
    build: .
    container_name: boost-ip-bot
    restart: unless-stopped
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://0.0.0.0:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
